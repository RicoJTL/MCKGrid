# Database
DATABASE_URL=postgresql://user:password@localhost:5432/mckgrid

# Server
PORT=5000
NODE_ENV=development
SESSION_SECRET=change-me

# Auth (will replace Replit later)
REPL_ID=your-replit-client-id
ISSUER_URL=https://replit.com/oidc

# Object Storage (will replace later)
PUBLIC_OBJECT_SEARCH_PATHS=/objects/uploads
PRIVATE_OBJECT_DIR=uploads
